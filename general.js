// Adicione um event listener para o botão do menu mobile
document.querySelector(".buttonMenuMobile").addEventListener("click", function () {
    // Alterne a classe "menuMobileAberto" no body
    document.body.classList.toggle("menuMobileAberto");
});